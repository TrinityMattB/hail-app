# Anthropic API Key
# The app calls the Anthropic API directly from the browser.
# For production, route API calls through your own backend to keep the key secret.
VITE_ANTHROPIC_API_KEY=your_api_key_here
